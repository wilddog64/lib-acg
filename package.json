{
  "name": "lib-acg",
  "version": "0.1.0",
  "description": "ACG browser automation — Chrome CDP, Playwright, credential extraction",
  "private": true,
  "scripts": {
    "check": "node --check playwright/*.js playwright/lib/*.js playwright/providers/*.js",
    "test": "jest"
  },
  "dependencies": {
    "playwright": "^1.58.2"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
