# Bugfix: v0.1.5 — retry sandbox restart when Azure SP credentials absent

**Branch:** `feat/v0.1.5`
**Files:** `bin/acg-credential-test`

---

## Problem

ACG Azure sandboxes inconsistently provision Service Principal credentials.
Some instances come up with empty `Application Client ID` and `Secret` fields — only
`Username` and `Password` are present. When this happens:

1. `acg_credentials.js` outputs only `AZURE_USERNAME` / `AZURE_PASSWORD` (SP fields are empty → not written).
2. `acg-credential-test` falls through to `_az_portal_valid`.
3. `az login --username --password` fails with `AADSTS50126` because Pluralsight tenants enforce MFA.
4. `_azure_auth_failed` exits 1 — user must manually delete the sandbox and retry.

The user must delete and recreate the sandbox multiple times until ACG provisions one
with SP credentials.

**Root cause:** The portal-only fallback path has no retry logic. When portal login fails
and SP credentials are absent, the script gives up instead of restarting the sandbox
to get a fresh instance that may have SP credentials provisioned.

---

## Fix

### Change 1 — `bin/acg-credential-test`: wrap Step 3 Azure validation in a retry loop

When portal-only (no SP) AND portal login fails, restart the sandbox and re-extract
credentials (up to 3 times). SP and identity failures remain permanent (wrong credentials
— retrying won't help).

**Exact old block (lines 264–284):**

```bash
# Step 3: validate Azure creds — CLI auth first, portal/TAP only when no CLI path exists
if grep -q '^AZURE_CLIENT_ID=' "$_tmpout" && grep -q '^AZURE_CLIENT_SECRET=' "$_tmpout"; then
  if _az_sp_valid; then
    printf 'INFO: Azure SP credentials validated (az login + token probe OK)\n' >&2
    _write_azure_credentials
  else
    _azure_auth_failed 'ERROR: Azure CLI auth failed — service-principal credentials invalid after all attempts.'
  fi
elif grep -q '^AZURE_CLIENT_ID=' "$_tmpout"; then
  if _az_identity_valid; then
    printf 'INFO: Azure identity credentials validated (az login + token probe OK)\n' >&2
  else
    _azure_auth_failed 'ERROR: Azure CLI auth failed — identity credentials invalid after all attempts.'
  fi
elif grep -q '^AZURE_USERNAME=' "$_tmpout" && grep -q '^AZURE_PASSWORD=' "$_tmpout"; then
  if _az_portal_valid; then
    printf 'INFO: Azure portal credentials validated (az login + token probe OK)\n' >&2
  else
    _azure_auth_failed 'ERROR: Azure CLI auth failed — portal credentials invalid after all attempts.'
  fi
fi
```

**Exact new block:**

```bash
# Step 3: validate Azure creds — CLI auth first, portal/TAP only when no CLI path exists
# Retry up to 3 times when portal-only sandbox lacks SP credentials (ACG provisioning gap).
_azure_sp_retries=0
_azure_validate_done=false
while [[ "$_azure_validate_done" == "false" ]]; do
  if grep -q '^AZURE_CLIENT_ID=' "$_tmpout" && grep -q '^AZURE_CLIENT_SECRET=' "$_tmpout"; then
    if _az_sp_valid; then
      printf 'INFO: Azure SP credentials validated (az login + token probe OK)\n' >&2
      _write_azure_credentials
    else
      _azure_auth_failed 'ERROR: Azure CLI auth failed — service-principal credentials invalid after all attempts.'
    fi
    _azure_validate_done=true
  elif grep -q '^AZURE_CLIENT_ID=' "$_tmpout"; then
    if _az_identity_valid; then
      printf 'INFO: Azure identity credentials validated (az login + token probe OK)\n' >&2
    else
      _azure_auth_failed 'ERROR: Azure CLI auth failed — identity credentials invalid after all attempts.'
    fi
    _azure_validate_done=true
  elif grep -q '^AZURE_USERNAME=' "$_tmpout" && grep -q '^AZURE_PASSWORD=' "$_tmpout"; then
    if _az_portal_valid; then
      printf 'INFO: Azure portal credentials validated (az login + token probe OK)\n' >&2
      _azure_validate_done=true
    else
      _azure_sp_retries=$(( _azure_sp_retries + 1 ))
      if (( _azure_sp_retries > 3 )); then
        _azure_auth_failed "ERROR: Azure portal auth failed and SP credentials absent after ${_azure_sp_retries} sandbox restarts — MFA enforcement prevents portal login."
      fi
      printf 'WARN: Azure portal-only sandbox with MFA enforcement — SP not provisioned (attempt %d/3). Restarting sandbox...\n' \
        "$_azure_sp_retries" >&2
      _do_restart "$@"
      _wait_cdp_ready
      _extract_credentials "$@" || {
        printf 'ERROR: Credential extraction failed after restart.\n' >&2
        exit 1
      }
      _print_masked
    fi
  else
    _azure_validate_done=true
  fi
done
```

---

## Files Changed

| File | Change |
|------|--------|
| `bin/acg-credential-test` | Wrap Step 3 Azure validation in retry loop; restart sandbox when portal-only + MFA fails |

---

## Rules

- `shellcheck -S warning bin/acg-credential-test` — zero new warnings
- No other files touched
- Do NOT modify `playwright/` or any Node files

---

## Definition of Done

- [ ] Step 3 Azure validation is wrapped in a while loop with `_azure_validate_done` flag
- [ ] Portal-only + portal-fails path restarts sandbox and re-extracts up to 3 times
- [ ] SP and identity failure paths remain permanent (unchanged logic)
- [ ] Counter message says `attempt N/3`
- [ ] `shellcheck -S warning bin/acg-credential-test` passes with zero new warnings
- [ ] Committed and pushed to `feat/v0.1.5`
- [ ] memory-bank updated with commit SHA and task status (in k3d-manager repo)

**Commit message (exact):**
```
fix(credential-test): retry sandbox restart when Azure SP credentials absent
```

---

## What NOT to Do

- Do NOT create a PR yourself — Claude handles PR creation after verifying the commit
- Do NOT skip pre-commit hooks (`--no-verify`)
- Do NOT modify any file other than `bin/acg-credential-test`
- Do NOT commit to `main` — work on `feat/v0.1.5`
- Do NOT touch the SP or identity failure paths — only the portal-only retry is new
