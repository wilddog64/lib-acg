# Retrospective — v0.1.1

**Date:** 2026-06-05
**Milestone:** sandbox navLink.click() regression fix
**PR:** #36 — merged to main (`f63b7ca3`)
**Participants:** Claude, Codex, Copilot

## What Went Well
- Root cause was correctly identified from browser screenshot (s2.pluralsight.com/404.html) alone
- Fix was minimal and exact — one conditional block removed, matching pre-refactor behavior
- Codex implemented correctly on first attempt
- CI green on first push; Copilot found only doc wording issues, not code issues

## What Went Wrong
- Codex skipped the memory-bank update (step 9 of spec DoD) — Claude had to fill it in manually
- Subtree discipline violation: Claude initially edited `scripts/lib/acg/playwright/lib/sandbox.js` directly in k3d-manager before being corrected — reverting required `git checkout -- <file>`
- Bug spec used boilerplate "No other files touched" and "Do NOT create a PR" lines that are internally inconsistent for a committed spec doc — Copilot caught both

## Process Rules Added

| Rule | Where |
|------|-------|
| Bug spec "Rules" section: use "Code change limited to `<file>`; bug spec doc and memory-bank updates are also expected" | Bug spec template |
| Bug spec "What NOT to Do": use "Do NOT create a PR yourself — Claude handles PR creation after verifying the commit" (Codex guard only; Claude still creates a PR as part of the normal workflow) | Bug spec template |

## Decisions Made
- lib-acg branches now follow versioned convention `feat/v<version>` instead of `docs/next-improvements` — matches k3d-manager milestone branch pattern, gives clear traceability

## Theme
A one-line regression introduced during the PR #35 provider pattern refactor: Codex re-added a `navLink.click()` path that the original author had explicitly removed with a comment explaining why. The fix was straightforward once identified — the hard part was catching that the subtree must never be edited directly, and that the bug spec template had two contradictory boilerplate lines that Copilot correctly flagged.
