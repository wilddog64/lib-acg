# Retrospective — v0.4.0

**Date:** 2026-06-10
**Milestone:** Azure sandbox provider + Playwright fix sweep
**PR:** #41 — merged to main
**Participants:** Claude, Gemini, Copilot

## What Went Well
- Gemini successfully investigated and filed 25+ bug reports for Playwright/credential issues
- Azure sandbox provider support added with full credential extraction (SP, portal TAP, CLI)
- CI remained green throughout (10/10 tests)
- Copilot caught version inconsistency (package.json vs CHANGELOG mismatch) before merge
- Merge conflict resolution on squash-diverged branch was straightforward

## What Went Wrong
- PR #39 was squash-merged which caused `feat/v0.1.4` to diverge — required manual merge conflict resolution
- Version inconsistency slipped past initial commits (package.json, README, docs/releases.md all needed bumping)
- lib-acg `CLAUDE.md` subtree still references old playwright layout (no `lib/sandbox.js` path listed)

## Process Rules Added
| Rule | Source |
|------|--------|
| Bump package.json + package-lock.json when CHANGELOG version changes | Copilot finding on PR #41 |
| Update README release table + docs/releases.md together with CHANGELOG | Copilot finding on PR #41 |

## Decisions Made
- Azure credential extraction uses exclusive auth-branch logic: SP → portal TAP → CLI auth (in priority order)
- OIDC tenant discovery via `/.well-known/openid-configuration` endpoint
- Screenshot archival goes to shared dir (not temp) for post-run inspection

## Theme
v0.4.0 was a Gemini-led investigation sprint. After repeated acg-up failures reported by the user, Gemini investigated the ACG Playwright scripts and systematically filed bugs across 81 commits on feat/v0.1.4. The milestone added Azure sandbox provider support and swept through 25+ Playwright/credential extraction edge cases. The primary bottleneck was merge conflict resolution after a squash-merge divergence, which Claude handled directly. Copilot caught one version consistency issue pre-merge.
